#!/usr/bin/env bash
# Git pre-commit hook for travis Harness
# Install: ln -sf ../../hooks/pre-commit .git/hooks/pre-commit
set -euo pipefail
REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && git rev-parse --show-toplevel)"
exec bash "$REPO_ROOT/test/validate.sh"
